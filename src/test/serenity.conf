webdriver {
    driver = "chrome",
    timeouts {
        pageLoad = 30000,
        implicit = 20000
    }
}

environments {
    default {
        webdriver.base.url = "https://www.google.com"
    }
}